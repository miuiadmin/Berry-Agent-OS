# Berry 运行时环境变量（全部可选——缺省值见 docs/使用指南.md §5）
# 模型覆盖（格式 provider/model-id；缺省 anthropic/claude-sonnet-5）
#APP_MODEL=anthropic/claude-sonnet-5
# 数据目录覆盖（缺省 ~/.berry）
#APP_DATA_DIR=
# SQLite 库文件路径覆盖（缺省 <数据目录>/sessions.db）
#APP_DB_PATH=
# 日志级别 debug/info/warn/error（dev 入口缺省 debug，其余 info）
#APP_LOG_LEVEL=
# provider 凭证走 pi-ai 凭证链（凭证表或 provider 环境变量，如 ANTHROPIC_API_KEY）
