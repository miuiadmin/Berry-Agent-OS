# 13.0 架构总览

> 13.0 是 BerryAgent 后端的灵魂版迭代。本文档面向**新加入项目的开发者**，
> 用 10 分钟让你理解 13.0 的核心架构、数据流和扩展点。

## 一句话总结

**13.0 = Brain 灵魂版 + 多 Agent 协作基础设施 + 完整安全门控 + 用户反馈闭环。**
Brain 是观察者 + 调度员 + 仲裁员，**永远不直接回复用户**。

## 架构全景

```
                                ┌─────────────────────────────────────┐
                                │       9 个 Agent（内核）            │
                                │                                     │
                                │  常驻：brain, conversation           │
                                │  按需：learning, skills,             │
                                │       plugin-builder, code,         │
                                │       evolution, memory,            │
                                │       skill-tester                  │
                                └─────────────────────────────────────┘
                                            │
                              ┌─────────────┴─────────────┐
                              ▼                           ▼
                    ┌──────────────────┐         ┌─────────────────────┐
                    │  AgentPort (6原语) │         │  Kernel（基础设施）│
                    │  + requestStreaming│        │  - MissionManager   │
                    │   v2              │         │  - DelegationMgr    │
                    └──────────────────┘         │  - DialogueRouter   │
                              │                  │  - KernelRouter      │
                              │                  │  - StateCache        │
                              ▼                  │  - PermissionCoord   │
                    ┌──────────────────┐         │  - TaskHeartbeat     │
                    │  IPC + EventBus    │◀────────│  - UserSessionQueue │
                    └──────────────────┘         │  - BrainDecisionRec  │
                              │                  │  - CorrectionFreqDet│
                              ▼                  │  - CorrectionEscDet │
                    ┌──────────────────┐         │  - SensitiveRedactor │
                    │  SQLite (事实源)   │◀────────│  - EvolutionEngine  │
                    │  + mission/       │         └─────────────────────┘
                    │    plan.json/     │                       │
                    │    squad.json     │                       │
                    └──────────────────┘                       │
                              ▲                              ▼
                              │                  ┌─────────────────────┐
                              │                  │  Web (Hono + WS)      │
                              │                  │  - REST API (10+)     │
                              └──────────────────┤  - WS 事件桥          │
                                                 │  - SPA 嵌入式         │
                                                 └─────────────────────┘
```

## 核心设计原则

### 1. Brain 不直接回复用户

- 所有用户消息经 Conversation Agent 处理
- Brain 只做：调度（谁来做）+ 组织（mission/squad）+ 审核（approve/modify/reject）+ 纠偏（中途干预）
- 任何 agent 都不能 `to: 'brain'` 发送消息（KernelRouter.gate() 硬阻止）

### 2. 数据流：DB 是事实源，JSON 文件是缓存

- Mission / Squad / HandoffContext 持久化到 `~/.berry/missions/<mission_id>/plan.json` + `squad.json`
- Brain decisions / corrections / user preferences 持久化到 SQLite
- Session 历史持久化到 `conversations` 表
- Skill / Plugin / Config 以文件为事实源

### 3. 模块化解耦（contract-first）

```
模块A ─────────── contract ─────────── 模块B
   │                                     │
   └─ 监听 EventBus 事件 ←───────────────┘
```

- 跨模块通信：EventBus / IPC / StateCache
- 不允许跨模块直接 import 内部实现
- 每个模块必须能独立单测

### 4. 安全三层防护

```
Layer 1: PermissionEngine.checkPermission (allow-all / ask / deny-all)
              ↓
Layer 2: PermissionCoordinator.checkActiveScope (硬约束 blockTools/blockPaths)
              ↓
Layer 3: 特定 tool 自己的检查（如 run_command 的 blocklist）
```

## 数据流：一个用户消息的一生

```
1. User 发送消息
   ↓
2. WS 收到 → kernel/handlers/unified-handlers.ts
   ↓
3. PendingRequest 创建 + SessionManager 保存
   ↓
4. Brain.route.request IPC → Brain Agent 思考
   ├─ 简单消息 → FallbackRouter 直发
   ├─ 复杂消息 → Brain LLM 决定 intent + targetAgent
   └─ mission 任务 → Brain LLM 创建/更新 plan
   ↓
5. KernelRouter.gate() 安全检查 (to ≠ 'brain', 频率限制, 调用深度)
   ↓
6. DialogRouter / Conversation Agent 处理
   ├─ Conversation 自己回答
   ├─ Delegate 给 Code/Learning/Skills 等按需 Agent
   └─ 触发 Plan Task → MissionManager.updatePlan
   ↓
7. Brain.review.request IPC → Brain 审核
   ↓
8a. BrainDecisionRecorder.recordReviewDecision (outcome=good/bad/neutral)
8b. MissionManager.updatePlan 自动 mark task done/failed
   ↓
9. BrainDecisionRecorder.recordReviewDecision → sensitive data 自动脱敏
   ↓
10. WS 推送 review_info → 前端显示审核结果
    └─ 用户可点击「还原 Brain 修改」→ POST /api/brain/restore-original
       ├─ 触发 brain.feedback 事件
       └─ Evolution Engine stub → PatternMatcher → user_preferences
```

## 关键文件导航

| 你想了解... | 看这个文件 |
|---|---|
| 三级 Agent 协作基础设施 | `src/agents/agent-port.ts` |
| Brain 审核/纠偏/调度 | `src/agents/bundled/brain/entry.ts` |
| Mission/Squad 生命周期 | `src/kernel/mission-manager.ts` |
| 跨 Agent 消息 | `src/kernel/dialogue-router.ts` |
| 安全门控 | `src/kernel/permission-coordinator.ts` |
| 升级式纠偏 | `src/kernel/correction-escalation-detector.ts` |
| 频次触发 Evolution | `src/kernel/correction-frequency-detector.ts` |
| 事件总线 | `src/kernel/event-bus.ts` |
| 用户偏好 | `src/memory/user-preferences.ts` |
| WS 事件推送 | `src/web/ws-event-bridge.ts` |
| REST API | `src/kernel/mission-api.ts` + `src/web/api-routes.ts` |
| 共享类型/契约 | `src/contracts/messages.ts` |

## 新增工具（13.0）

22 个 builtin tools，分两组：

| builtin（系统提供） | plugin（运行时注册） |
|---|---|
| read_file, write_file, list_directory, delete_file | 任何 plugin.register_tools IPC 注册的 |
| run_command, http_fetch, search_files, grep_files | |
| web_search, web_fetch, ask_user, push_notification | |
| monitor_start/stop/status, cron_create/delete/list | |
| search_history, plan, squad | |

API：`listBuiltinTools()` vs `listPluginTools()`，UI 可分组展示。

## 新增数据结构

### HandoffContext（§5.3.11）

```ts
interface HandoffContext {
  originalInstruction: string;        // 原始用户指令
  intentAnchor: { goal, successCriteria, scope } | null;
  filesRead: string[];                // 已读文件
  filesModified: Array<{ path, diffHash? }>;
  agentConversations: Array<{ with, summary, at }>;
  currentProgress: string;
  blockers: Array<{ reason, raisedAt, raisedBy }>;
  scopeSnapshot?: { blockPaths, blockTools };
  handoffAt: number;
  fromAgent: string;
}
```

### MissionContext（§12.3）

```ts
interface MissionContext {
  missionId, goal: string;
  currentTaskId, currentTaskWhat: string;
  squadRole: 'lead' | 'work' | 'check';
  squadOn, squadGoal: string;
  squadTeammates: Array<{ agent, role, on, status }>;
  completedTasks, inProgressTasks: Array<{ id, what, result? }>;
  unresolvedSignals: Array<{ from, type, msg, at }>;
}
```

## 新增 EventBus 事件类型

| 事件 | 触发时机 | 订阅方 |
|---|---|---|
| `mission.created/updated/completed/squad_created/handoff` | MissionManager 操作 | WS 推送 + UI |
| `brain.correction` | CorrectionFlow.applyAdjust | WS 推送 + UserSessionQueue + 持久化 |
| `brain.feedback` | 用户点「反馈 Brain 修改」 | Evolution Engine stub → user_preferences |
| `brain.checker.dispatch` | Squad check 角色触发 | Kernel 路由 review.request |
| `brain.signal_intervention` | Brain 观察 blocker signal | Kernel 路由 turn.correction |
| `capability.evolution.request` | 频次/反馈/learning 三类 | Evolution 引擎 |
| `user.session.queued/dequeued` | UserSessionQueue 操作 | 前端等待提示 |
| `cron.review` | Cron 任务完成 | Brain 独立审核 |
| `task.heartbeat` | 60s 无活动 | WS 推送 |
| `delegation.completed/failed` | Delegation 生命周期 | Kernel 清理 active_scope |

## 新增数据表

| 表 | 用途 | 持久化 |
|---|---|---|
| `brain_decisions` | Brain 决策历史（route/review/permission/correction/aggregated_insight/will_action） | ✅ |
| `brain_corrections` | 纠偏频次追踪 | ✅ |
| `brain_observations` | Brain 观察队列（500 条滚动窗口） | ✅ |
| `user_preferences` | 跨 session 用户偏好 + 90 天过期 | ✅ |
| `agent_chat_messages` | Agent 间对话审计 | ✅ |
| `agent_tool_calls` | 工具调用审计 | ✅ |
| `intent_anchors` | 12.0 意图锚 | ✅ |

## 新增 REST API

```
GET    /api/missions                       — 列出所有 mission
GET    /api/missions/:id                   — mission 详情
GET    /api/missions/:id/summary           — 摘要
GET    /api/missions/:id/squad             — squad 结构
GET    /api/missions/:id/signals           — 信号流
GET    /api/missions/templates             — 模板列表
POST   /api/missions/:id/signal            — 发送信号

POST   /api/brain/restore-original         — 还原 Brain 修改（§5.3.12）
POST   /api/brain/feedback                 — 反馈 Brain（§5.3.4）

GET    /api/agent-chat/:sessionId          — Agent 间对话
GET    /api/preferences?userId=&keyPrefix= — 列出偏好
POST   /api/preferences                    — 设置偏好
DELETE /api/preferences/:key?userId=      — 删除偏好
GET    /api/drift/metrics                  — 12.0 漂移指标
```

## 常见场景

### 场景 A：用户问简单问题

```
User → WS → KernelRouter → Conversation Agent
                       ↓
                     LLM 直接回答
                       ↓
                Brain review (auto-approve for A级)
                       ↓
                  final.response → WS → 前端
```

### 场景 B：用户问代码问题

```
User → WS → KernelRouter → Brain.route.request
                              ↓
                            Brain LLM: intent=code
                              ↓
                          Brain sends review.request to code agent
                              ↓
                          Code Agent 通过 AgentPort 用工具
                              ↓
                          Code Agent 提交 turn.final
                              ↓
                          Brain.review (B级审核)
                              ↓
                          Conversation 包装回复
                              ↓
                          final.response → WS
```

### 场景 C：用户启动复杂 mission

```
User → WS → KernelRouter → Brain.route.request
                              ↓
                            Brain LLM: 创建 mission spec
                              ↓
                          MissionManager.createMission()
                          + autoGenerateSquad()
                              ↓
                          EventBus emits mission.created/updated
                              ↓
                          Frontend 显示 mission UI
                              ↓
                          mission.task_ready → 自动派发到 Code/Learning agent
                              ↓
                          Plan tasks 按依赖顺序执行
                              ↓
                          每完成一个 → BrainDecisionRecorder 记录 + plan auto-mark-done
                              ↓
                          全部完成 → mission.completed 事件
```

### 场景 D：worker 失败触发纠偏

```
Code Agent 失败（连续 3 次或预算超限）
                              ↓
                          CorrectionFlow.handleCheckpointNeeded
                              ↓
                          Brain.checkpoint.evaluate
                              ↓
                          Brain LLM 决定 action=adjust + instruction
                              ↓
                          applyAdjust:
                            ├─ PermissionCoordinator.setActiveScope(blockTools)
                            ├─ StateCache.set('correction', lesson)
                            ├─ BrainDecisionRecorder.recordReviewDecision
                            ├─ CorrectionFrequencyDetector.record (累积)
                            └─ EventBus.emit('brain.correction')
                              ↓
                          Code Agent 通过 PermissionCoordinator.checkActiveScope
                          拦截到 forbidden tools → 必须改用允许的工具
```

## 扩展指南

### 添加新 Agent

1. 在 `src/agents/bundled/<name>/` 创建目录
2. 写 `agent.json` manifest（参考 `brain/agent.json`）
3. 写 `entry.ts` 用 `startResidentAgent` 或 `startModuleAgent`
4. 在 `src/agents/index.ts` 注册

### 添加新 Tool

```ts
import type { ToolDefinition } from './types.js';
import { z } from 'zod';

export const myTool: ToolDefinition = {
  name: 'my_tool',
  description: '...',
  inputSchema: z.object({...}),
  dangerLevel: 'moderate',  // 'safe' | 'moderate' | 'dangerous'
  execute: async (input) => ({ content: '...' }),
};
```

然后在 `src/tools/index.ts` 添加到 `builtinTools` 数组。

### 添加新 EventBus 事件

1. 在 `src/contracts/messages.ts` 的 `EventMap` 添加类型
2. 在消费者端直接用 `getEventBus().emit('your.event', {...})`（类型安全，无需 `as any`）
3. 在 `src/web/ws-event-bridge.ts` 添加到 `BRIDGED_EVENTS` 或 `STREAM_EVENT_MAPPING`

### 添加新 Brain Review 场景

1. 在 `src/agents/bundled/brain/prompts.ts` 加新场景描述
2. 在 `entry.ts` 的 `review.request` handler 加新分支
3. 写测试覆盖新场景

## 关键性能指标

- AgentPort.request() 默认超时 60s
- DialogueRouter per-target 上限 2 个对话
- MissionContext 注入由 Brain LLM 在每次 turn 开头读取（缓存于 StateCache）
- BrainDecisionRecorder 持久化 < 5ms
- brain_corrections 表查询（5min 窗口）< 10ms

## 调试技巧

| 现象 | 排查位置 |
|---|---|
| Agent 启动失败 | `~/.berry/agents/<name>/agent.yaml` |
| Tool 调用失败 | `~/.berry/logs/<sessionId>.log`（debug 模式） |
| Brain 决策异常 | `brain_decisions` 表 `WHERE session_id = ?` |
| 纠偏频繁 | `brain_corrections` 表 `WHERE agent_name = ? AND created_at > ?` |
| Mission 卡住 | `mission.task_ready` 事件 + `agent_tasks.output_payload` |
| WS 推送失败 | `src/web/ws-event-bridge.ts` STREAM_EVENT_MAPPING |

## 常用调试命令

```bash
# 启动 debug 模式
npm run dev:debug

# 查看 Brain 决策
sqlite3 ~/.berry/state.db "SELECT * FROM brain_decisions ORDER BY created_at DESC LIMIT 10;"

# 查看用户偏好
sqlite3 ~/.berry/state.db "SELECT * FROM user_preferences;"

# 查看活跃 mission
ls -la ~/.berry/missions/

# 查看最近纠正频次
sqlite3 ~/.berry/state.db "SELECT agent_name, COUNT(*), MAX(severity) FROM brain_corrections WHERE created_at > strftime('%s','now','-1 hour')*1000 GROUP BY agent_name;"
```

## 设计纪律（CLAUDE.md）

1. **无 if/else 补丁**：每个新概念解决多个问题
2. **无 backward-compat shim**：直接重构
3. **Brain 必经审核**：所有回复路径走 review
4. **模块化解耦**：通过 EventBus / IPC / StateCache
5. **命名去品牌化**：代码标识符无 `berry` 等品牌词
6. **中文 JSDoc + 行内注释**：新代码注释齐全
7. **测试覆盖**：所有新功能必有单测
8. **pre-commit hook**：typecheck + 全测试套件100% 通过