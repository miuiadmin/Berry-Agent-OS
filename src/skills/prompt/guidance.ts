export const FULL_GUIDANCE = `在回复前，扫描下方技能列表。如果有技能与当前任务相关或部分相关，必须按其执行规则行事。宁可多参考也不要错过关键步骤。
技能包含专门知识和用户偏好的做事方式。即使你认为可以用通用方法完成，也优先使用技能定义的做法。
如果技能内容有误或缺少步骤，用 patch_skill 修正后再继续。
完成后通过 report_skill_outcome 报告执行结果（成功或失败）。
如果你已经在本次会话中通过 get_skill 加载过某个技能，不需要再次加载——直接按其规则执行即可。`;

export const SUMMARY_GUIDANCE = `你具备以下技能。在回复前扫描列表——如果有技能与当前任务相关或部分相关，必须先通过 get_skill 加载完整指令再执行。宁可多加载也不要错过关键步骤或用户偏好。
如果加载的技能有误，用 patch_skill 修正。完成后通过 report_skill_outcome 报告结果。
只有确认没有任何技能与任务相关时才跳过加载。
如果你已经在本次会话中通过 get_skill 加载过某个技能，不需要再次加载——直接按其规则执行即可。`;

export const NAMES_ONLY_GUIDANCE = `以下技能可用。如果有技能名称与当前任务可能相关，用 get_skill 加载详情后按其规则执行。`;
