/**
 * L5 app — 快速上手共享骨架（第五十一批 m4 尾账收口，2026-09-01 落）。
 *
 * /guide 常驻参考（commands.ts GUIDE_TEXT）与首启欢迎块（tui-main.ts）同源
 * 的行——默认应用名等改词**一处生效两处**（Warp 读码批实证：两份平行文本
 * 已发生一次漂移——coder→berrycode 只改了 guide 侧）。
 */

/** 首用指路核心句（默认应用 + 纯对话出口——两消费面共用，勿单侧改词） */
export const QUICK_START_ENTRY = '直接说需求即可——默认进入 berrycode 代码智能体应用（/app chat 换纯对话）';
